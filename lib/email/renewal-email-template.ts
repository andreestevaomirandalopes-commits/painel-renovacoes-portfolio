import type { Client } from "@prisma/client";
const escapeHtml=(value:string)=>value.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;");
export function renewalEmailHtml(client:Client, message:string) {
  const logo=process.env.EMAIL_LOGO_URL;
  const color=process.env.EMAIL_BRAND_COLOR||"#0f766e"; const brandName=process.env.EMAIL_SENDER_NAME||"Painel de Gestão"; const name=client.name||client.legalName||"Cliente"; const content=escapeHtml(message).replace(/\n/g,"<br />");
  const brandHeader=logo ? `<img src="${logo}" alt="${escapeHtml(brandName)}" style="display:block;max-width:220px;max-height:80px;width:auto;height:auto" />` : `<strong style="font-size:22px;color:${color}">${escapeHtml(brandName)}</strong>`;
  return `<!doctype html><html lang="pt-BR"><body style="margin:0;padding:0;background:#f3f6f8;font-family:Arial,Helvetica,sans-serif;color:#24313a"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background:#f3f6f8;padding:28px 12px"><tr><td align="center"><table role="presentation" width="600" cellspacing="0" cellpadding="0" style="width:100%;max-width:600px;background:#ffffff;border-radius:14px;overflow:hidden"><tr><td align="center" style="padding:24px;background:#ffffff;border-bottom:4px solid ${color}">${brandHeader}</td></tr><tr><td style="padding:32px 34px"><h1 style="margin:0 0 18px;font-size:25px;line-height:1.25;color:${color}">Renovação do seu certificado digital</h1><p style="margin:0 0 20px;font-size:16px;line-height:1.65">Olá, <strong>${escapeHtml(name)}</strong>.</p><p style="margin:0 0 22px;font-size:16px;line-height:1.65">${content}</p><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background:#f7fafb;border:1px solid #dce7eb;border-radius:8px"><tr><td style="padding:18px"><p style="margin:0 0 8px;font-size:13px;color:#63727b">DADOS DO CERTIFICADO</p><p style="margin:0 0 6px;font-size:15px"><strong>Pedido:</strong> ${escapeHtml(client.orderNumber)}</p><p style="margin:0 0 6px;font-size:15px"><strong>Produto:</strong> ${escapeHtml(client.productType)}</p><p style="margin:0;font-size:15px"><strong>Vencimento:</strong> ${client.expirationDate.toLocaleDateString("pt-BR")}</p></td></tr></table></td></tr><tr><td align="center" style="padding:22px 28px;background:#eef3f5;color:#63727b;font-size:12px;line-height:1.55">${escapeHtml(brandName)}<br />Mensagem demonstrativa</td></tr></table></td></tr></table></body></html>`;
}
