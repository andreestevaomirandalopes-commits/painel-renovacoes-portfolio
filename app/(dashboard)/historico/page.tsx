import { prisma } from "@/lib/prisma";
import { TableScroll } from "@/components/layout/table-scroll";
export default async function History() { const logs = await prisma.contactLog.findMany({ include: { client: true, user: true }, orderBy: { createdAt: "desc" }, take: 200 }); return <><h2 className="text-2xl font-bold mb-2">Histórico de contatos</h2><p className="text-slate-500 mb-5">Todos os envios realizados pelo painel.</p><div className="bg-white border rounded-xl overflow-hidden"><TableScroll><table><thead><tr><th>Data</th><th>Cliente</th><th>Canal</th><th>Destinatário</th><th>Responsável</th><th>Status</th></tr></thead><tbody>{logs.map(log=><tr key={log.id}><td>{log.createdAt.toLocaleString("pt-BR")}</td><td>{log.client.name||log.client.legalName||"—"}<br/><span className="text-xs text-slate-500">Pedido {log.client.orderNumber}</span></td><td>{log.type === "EMAIL" ? "E-mail" : "WhatsApp"}</td><td>{log.recipient}</td><td>{log.user.name}</td><td>{log.status === "SENT" ? "Enviado" : log.status === "FAILED" ? "Falhou" : "Pendente"}</td></tr>)}{logs.length===0&&<tr><td colSpan={6} className="text-slate-500">Nenhum envio registrado ainda.</td></tr>}</tbody></table></TableScroll></div></> }
