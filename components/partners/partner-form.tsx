"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function PartnerForm(){const [error,setError]=useState("");const router=useRouter();async function save(data:FormData){setError("");const response=await fetch("/api/partners",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(data))});const result=await response.json();if(!result.success){setError(result.error?.message||"Não foi possível cadastrar o parceiro.");return;}router.push("/parceiros");router.refresh()}return <form action={save} className="bg-white border rounded-xl p-6 grid grid-cols-1 md:grid-cols-2 gap-4"><label className="text-sm font-medium md:col-span-2">Nome Escritório<input name="officeName" required/></label><label className="text-sm font-medium">E-mail<input name="email" type="email" required/></label><label className="text-sm font-medium">Código REV<input name="revCode" required/></label>{error&&<p className="text-red-600 text-sm md:col-span-2">{error}</p>}<button className="brand-bg text-white rounded-lg px-4 py-2 w-fit">Salvar parceiro</button></form>}
