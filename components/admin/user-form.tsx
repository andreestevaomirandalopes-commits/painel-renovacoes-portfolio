"use client";
import { useState } from "react";
export function UserForm() { const [message,setMessage]=useState(""); async function submit(form:FormData) { const response=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(form))}); const body=await response.json(); setMessage(body.success ? "Usuário criado com sucesso." : body.error?.message || "Não foi possível criar o usuário."); if(body.success)(document.getElementById("user-form") as HTMLFormElement)?.reset(); } return <form id="user-form" action={submit} className="grid md:grid-cols-2 gap-3"><input name="name" required placeholder="Nome"/><input name="email" type="email" required placeholder="E-mail"/><input name="password" type="password" required minLength={8} placeholder="Senha (mínimo 8 caracteres)"/><select name="role"><option value="OPERADOR">Operador</option><option value="ADMIN">Administrador</option></select><button className="bg-teal-700 text-white rounded-lg px-4 py-2 w-fit">Criar usuário</button>{message&&<p className="text-sm self-center">{message}</p>}</form> }
